inherit: 
    - ../wandb.yaml

training:
    task: kalman_filter
    task_kwargs: {"hidden_layer_size": 4}
    curriculum:
        points:
            start: 1
            end: 13
            inc: 1
            interval: 2000

out_dir: ../models/kalman_filter

wandb:
    name: "kalman_filter"
