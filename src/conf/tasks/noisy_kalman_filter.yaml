inherit: 
    - ../wandb.yaml

training:
    task: noisy_kalman_filter
    task_kwargs: {"hidden_layer_size": 100}
    curriculum:
        points:
            start: 15
            end: 20
            inc: 1
            interval: 2000

out_dir: ../models/noisy_kalman_filter

wandb:
    name: "noisy_kalman_filter"
